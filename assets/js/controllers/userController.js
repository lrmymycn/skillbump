'use strict';

angular.module('skillBump').controller('userController', ['$scope', 'userResource', function ($scope, userResource) {
    /*
      userResource.getAll().then(function (response) {
        $scope.users = response.data;
      });
    */
}]);
